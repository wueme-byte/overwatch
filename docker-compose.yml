

services:
  redis:
  
    image: redis:7-alpine
    restart: always

  api:
    build: .
    command: uvicorn api:app --host 0.0.0.0 --port 8001
    ports:
      - "8001:8001"
    env_file: .env
    environment: 
       - HTTPS_PROXY=http://iaebFJaedrYXuIhc:kom2WDC6bxP7lKiJ@geo.iproyal.com:12321         
    depends_on:
      - redis
    restart: always

  bot:
    build: .
    command: python -m bot.main
    env_file: .env
    depends_on:
      - redis
    restart: always
